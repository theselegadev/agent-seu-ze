export type Barber = {
    id?: number,
    name: string,
    telefone: string,
    address: string,
    password: string
}

export type Client = {
    id?: number,
    name: string,
    telefone: string   
}